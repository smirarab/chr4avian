* `GO_enrichment.R` the analyses, given in R
	* Uses GO terms from <./gprofiler_full_ggallus.name.gmt>
* `drawTerms.R` draws the figure S11. 
* It uses  `WG.tsv` which summarizes the full results given in the excel file `WG_gprofile2_all_GO_results.xlsx`

